export interface CustomerListModel {
    id: String,
    name: String,
    country: String,
    scoring: Number
}

export interface CustomerDetailsModel {
    id: String,
    name: String,
    city: String,
    country: String,
    scoring: Number
}